#include "reloj.h"
#include <string.h>

reloj_t CrearReloj(uint16_t ticks_por_segundo){

}

bool TraerHoraReloj(reloj_t reloj, uint8_t * tiempo, uint8_t size){
    memset(tiempo, 0, size);
    return false;
}